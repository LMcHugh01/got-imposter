/**
 * data/site.js
 *
 * The site's name and tagline, in one place. The header, the home page and
 * any other page that names the site read them from here, so renaming the
 * site is a one-line change (plus the <title> in index.html).
 */
export const SITE = {
    name: 'Westerosity',
    tagline: 'Explore the realm. Play for the throne.',
  }
   